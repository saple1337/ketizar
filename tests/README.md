Vanilla hands-off tests are intentionally replaced by this mod.

The vanilla test files reference country tags and scripted triggers that this
total-conversion setup removes or renames. Loading them during hands-off runs
can abort the game with invalid trigger scopes.
