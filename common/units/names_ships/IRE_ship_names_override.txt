# OVERRIDE - Removes nonexistent IRE tag from ship naming
IRE_DD_HISTORICAL = {
	name = NAME_THEME_HISTORICAL_DESTROYERS
	for_countries = { }
	type = ship
	ship_types = { ship_hull_light destroyer }
	prefix = ""
	fallback_name = "Ship %d"
}
